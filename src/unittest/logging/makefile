# A sample Makefile for building Google Test and using it in user
# tests.  Please tweak it to suit your environment and project.  You
# may want to move it to your project's root directory.
#
# SYNOPSIS:
#
#   make [all]  - makes everything.
#   make TARGET - makes the given target.
#   make clean  - removes all files generated by make.

# Please tweak the following variable definitions as needed by your
# project, except GTEST_HEADERS, which you can use in your own targets
# but shouldn't modify.

# Points to the root of Google Test, relative to where this file is.
# Remember to tweak this if you move this file.
# Where to find user code.
LOGGING_USER_TEST_DIR = unittest/logging

# All tests produced by this Makefile.  Remember to add new tests you
# created to the list.
TESTS = $(OBJDIR)/httpc_logging_ut $(OBJDIR)/httpc_config_ut

# All Google Test headers.  Usually you shouldn't change this
# definition.
GTEST_HEADERS = $(GTEST_DIR)/include/gtest/*.h $(GTEST_DIR)/include/gtest/internal/*.h

# House-keeping build targets.

all : $(TESTS)

# Builds a sample test.  A test should link with either gtest.a or
# gtest_main.a, depending on whether it defines its own main()
# function.

$(OBJDIR)/httpc_logging_ut.o : $(LOGGING_USER_TEST_DIR)/httpc_logging_ut.cc  $(GTEST_HEADERS)
	$(CXX) $(CPPFLAGS) $(CXXFLAGS) -c $(LOGGING_USER_TEST_DIR)/httpc_logging_ut.cc -o $@

$(OBJDIR)/httpc_logging_ut : $(OBJDIR)/httpc_logging_ut.o $(OBJDIR)/httpc_config.o $(OBJDIR)/httpc_logging.o  $(OBJDIR)/httpc_stringutil.o $(OBJDIR)/gtest_main.a
	$(CXX) $(CPPFLAGS) $(CXXFLAGS) -lpthread $^ -o $@

$(OBJDIR)/httpc_config_ut.o : $(LOGGING_USER_TEST_DIR)/httpc_config_ut.cc  $(GTEST_HEADERS)
	$(CXX) $(CPPFLAGS) $(CXXFLAGS) -c $(LOGGING_USER_TEST_DIR)/httpc_config_ut.cc -o $@

$(OBJDIR)/httpc_config_ut : $(OBJDIR)/httpc_config_ut.o $(OBJDIR)/httpc_config.o  $(OBJDIR)/httpc_stringutil.o $(OBJDIR)/gtest_main.a
	$(CXX) $(CPPFLAGS) $(CXXFLAGS) -lpthread $^ -o $@


